import React from 'react';
import './Loading.scss';

const Loading = () => {
  return (
    <div className="loading-messaging" data-cy="loading">
      <h2>LOADING...</h2>
    </div>
  );
};

export default Loading;
